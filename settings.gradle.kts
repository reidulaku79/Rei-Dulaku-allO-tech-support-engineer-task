plugins {
    // Lets Gradle auto-download a matching JDK 25 toolchain if it is not installed locally.
    id("org.gradle.toolchains.foojay-resolver-convention") version "1.0.0"
}

rootProject.name = "orderdesk"
